%section
  %h3.items-box-head.underline-opacity
    =link_to "#{@item.user.nickname}のその他の出品", "#"
  .items-box-content
    = render "./common/items", items: Item.where.not(id: @item.id).where(seller_id: @item.user.id).first(6)
