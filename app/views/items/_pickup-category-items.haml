%section
  %h3.items-box-head.underline-opacity
    =link_to "#{small_category_name(@item)}のその他の出品", "#"
  .items-box-content
    = render "./common/items", items: Item.where.not(id: @item.id).where(small_category_id: @item.small_category_id).first(6)
