%section.items-box-container
  %h3.text-center=link_to "#{section_name}新着アイテム", root_path
  .items-box-content
    - items.each do |item|
      = render "items", item: item
  .view-all=link_to "すべての商品を見る", root_path
